greet = fn[name] {
    .print["Hello, " + name + "!"];
};

.greet["world"];

curry = fn[a] {
    .print[a];
    return = fn[b] {
        return = a + b;
    };
};

addFive = curry[5];
.print[addFive[4]];

f = fn [x] {
    .print[x];
};
a = 5;
b = 3;
if a > b {
    while b < (a * 3) {
        c = 1;
        b = b + c;
    }
} else if a == b .print[7]
  else .print[9];
.print[b]; // prints 15
hello = "hello " + "world!";
should hello == "hello world!" {
    .f[5];
    .print["I'm gonna fail now, with a red error"];
    .print[x];
}